# Gitコミットを分割する技術：歴史を綺麗に整理しよう

*ここに見出し画像を挿入*

{
  "ja": "Gitのコミット履歴を後から整理する方法を解説。複数の変更を1つのコミットにまとめたり、分割したりする技術をマスターすると、プロジェクトの歴史が見やすくなり、チームでのコラボレーションがスムーズに。この記事では、具体的なコマンドとベストプラクティスを紹介し、実践的な活用法を紹介します。"
}

{
  "ja": "## 📌 このトピックの本質\n\nGitのコミット履歴はプロジェクトの「歴史」そのもの。しかし、開発が進むにつれて、1つのコミットに複数のロジックが混ざったり、小さな変更がまとめられすぎたりすることで、履歴が読みづらくなります。この記事では、**`git rebase` や `git add -p` などのツールを使って、既存のコミットを分割・再構築する方法**を解説。コミット履歴を整理することで、バグトラッキングやレビューが効率化し、チーム全体の生産性が向上します。\n\n## 🎯 5秒で分かるポイント\n- **Point 1**: `git add -p`（パッチモード）で、ファイル内の変更を1行ずつ選択してコミットできる\n- **Point 2**: `git rebase -i`（インタラクティブリベース）で、過去のコミットを編集・分割・削除できる\n- **Point 3**: 分割したコミットは**新しいコミットハッシュ**が生成され、履歴が変わるため、共有前には`git push --force`を使用する\n\n## 📊 詳細解説\n\n**Element 1：コミットを分割する方法（`git add -p`）**\n\n`git add -p`は、ファイル内の変更を**インクリメンタルに選択**できる強力なツールです。例えば、1つのファイルで複数のロジックを変更した場合、このコマンドを使うと、**1つのコミットにまとめずに、それぞれの変更を個別にコミット**できます。具体的には、コマンドを実行すると、変更のパッチが表示され、ユーザーは「y」または「n」でそのパッチをコミットに含めるかどうか選択できます。これにより、コミット履歴が**細かすぎない**ほどの粒度で管理できます。\n\n**Element 2：過去のコミットを編集する（`git rebase -i`）**\n\n`git rebase -i`は、過去のコミットを**再編集**できる機能です。例えば、古いコミットに複数の変更が含まれていた場合、このコマンドを使って、**そのコミットを分割したり、順序を変更したり、メッセージを修正したり**できます。操作はテキストエディタで行い、コミットのハッシュを編集して実行します。ただし、**注意点**として、リベースは**共有されているブランチで行うと、他の開発者に影響を与える**ため、必ず`git push --force`を使用するか、ローカルで行うようにしましょう。\n\n> 💡 ポイント: コミットを分割する際は、**1つのコミットは1つの機能や修正に専念する**という原則（[Atomic Commit](https://www.atlassian.com/git/tutorials/rewriting-history/git-rebase)）を守ることが重要。これにより、履歴が読みやすくなり、バグの特定も容易になります。\n\n## 🚀 実世界への影響\n- **チームのコラボレーションがスムーズになる**: コミット履歴が整理されていると、他のメンバーが変更の内容を理解しやすくなり、レビューやマージが高速化します\n- **バグトラッキングが効率化**: 1つのコミットに複数の変更が含まれていないため、バグの発生源を特定しやすくなり、修正も迅速に行えます\n- **プロジェクトの品質向上**: 小さなコミットは、テストやレビューの負担を軽減し、コードの品質を向上させます\n\n## ✨ 結論\n\nGitのコミット履歴を分割・整理する技術は、開発者にとって**必須のスキル**です。`git add -p`や`git rebase -i`を活用することで、プロジェクトの歴史を**綺麗で読みやすいもの**に変えられます。ただし、リベースは慎重に行い、共有ブランチへの影響を最小限に抑えることが大切です。ぜひ、この技術を日常の開発に取り入れ、チーム全体の生産性を向上させましょう。"
}
