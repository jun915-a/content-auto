# **PyPI Blog: リリースが14日以内に新しいファイルを拒否するようになった**

*ここに見出し画像を挿入*

PyPI が新しいリリースポリシーを導入し、14 日以内に新しいファイルが追加されない場合にリリースを拒否するようになりました。これにより、PyPI の安全性と信頼性が向上します。

## 📌 このトピックの本質
PyPI は、14 日以内に新しいファイルが追加されない場合にリリースを拒否するようになりました。これは、PyPI の安全性と信頼性を向上させるためです。

## 🎯 5秒で分かるポイント
- **Point 1**: 14 日以内に新しいファイルが追加されない場合にリリースを拒否する
- **Point 2**: PyPI の安全性と信頼性が向上する
- **Point 3**: このポリシーにより、PyPI のユーザが安全にパッケージをインストールできるようになる

## 📊 詳細解説
**パッケージの安全性**
PyPI は、パッケージを安全にインストールできるようにするために、14 日以内に新しいファイルが追加されない場合にリリースを拒否することが決定しました。これにより、PyPI のユーザが安全にパッケージをインストールできるようになります。

**PyPI の信頼性**
このポリシーにより、PyPI の信頼性が向上します。ユーザが安全にパッケージをインストールできるため、PyPI の信頼性が高まります。

> 💡 ポイント: PyPI の安全性と信頼性が向上します。

## 🚀 実世界への影響
- このポリシーにより、PyPI のユーザが安全にパッケージをインストールできるようになる
- PyPI の安全性と信頼性が向上する
- このポリシーにより、PyPI の信頼性が高まる

## ✨ 結論
PyPI は、14 日以内に新しいファイルが追加されない場合にリリースを拒否するようになりました。これにより、PyPI の安全性と信頼性が向上します。ユーザが安全にパッケージをインストールできるようになるため、PyPI の信頼性が高まります。
