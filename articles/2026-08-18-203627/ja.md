# Gitのコミットを分割する技術：歴史を綺麗に整える方法

{
  "ja": "Gitのコミット履歴を後から分割する方法をマスターすれば、過去のコミットを整理し直すことが可能になり、プロジェクトのクリーンな歴史を保てます。この記事では、ブログ記事を元に、実践的な手法と注意点を解説。開発者必見のテクニックです。"
}

{
  "ja": "## 📌 このトピックの本質\n\nGitのコミット履歴はプロジェクトの進化を反映する重要な資産です。しかし、複数の変更を1つのコミットにまとめてしまうと、将来的なメンテナンスやバグトラッキングが難しくなります。この記事では、**`git rebase -i`** や **`git add -p`** などのコマンドを活用して、既存のコミットを後から分割・整理する方法を解説。コミット履歴を綺麗に保つための実践的なテクニックを紹介します。\n\n\n## 🎯 5秒で分かるポイント\n- **Point 1**: **`git rebase -i`** を使えば、過去のコミットを選択・編集・削除できる\n- **Point 2**: **`git add -p`** で、ファイルの一部変更を個別のコミットに分割可能\n- **Point 3**: 分割後のコミットは、**`git log --graph`** で視覚的に確認できる\n\n\n## 📊 詳細解説\n\n**Element 1: `git rebase -i` でのコミットの再構築**\n\n`git rebase -i` は、インタラクティブリベースツールで、指定したコミット以降の履歴を編集できます。例えば、古いコミット `abc123` から `def456` までの履歴を分割したい場合、以下の手順で行います。\n\n- まず、編集したい範囲のコミットハッシュを指定して実行します。\n- 開かれたエディタで、コミットの順序や内容を変更できます。`pick` を `edit` に置き換えることで、特定のコミットを停止し、詳細な編集を行えます。\n\n> 💡 ポイント: **リベースは共有ブランチに対して行わないこと**。リベースは再実行不可能な変更を生み出すため、他の開発者と共有する前に必ず **`git push --force`** を避けるか、コミュニケーションを取りましょう。\n\n\n**Element 2: `git add -p` での微細な変更のコミット分割**\n\nファイルの一部変更だけを個別のコミットに分割したい場合、`git add -p` が便利です。このコマンドは、ファイルの変更を **パッチ単位** で選択できるため、ロジカルなコミット履歴を作成できます。\n\n- `git add -p` を実行すると、変更の一部分を選択するプロンプトが表示されます。\n- 選択したパッチをコミットすることで、細かい変更を個別に記録できます。\n\n> 💡 ポイント: **`git add -p` は、大規模な変更ファイルに特に有効**。例えば、1つのファイルに複数のロジックが混在している場合、このツールで分割できます。\n\n\n## 🚀 実世界への影響\n- **チーム開発のクリーンな履歴**: コミット履歴が整理されていれば、他のメンバーも過去の変更を理解しやすくなり、バグトラッキングが効率化\n- **バグ修正の精度向上**: 具体的なコミットに対応した変更を特定できるため、原因追跡がスムーズ\n- **プロジェクトの信頼性向上**: 綺麗な履歴は、新規加入者や外部のレビュアーに対してもプロジェクトの品質を示す\n\n\n## ✨ 結論\n\nGitのコミット履歴を後から分割することは、プロジェクトの健全性を保つための重要なスキルです。`git rebase -i` や `git add -p` を活用することで、過去の変更を整理し直し、将来の開発をスムーズに進められます。ただし、リベースは慎重に行い、共有ブランチへの影響を最小限に抑えることが大切です。綺麗な履歴を維持する習慣を身につけ、チーム全体の開発効率を向上させましょう。"
}
